def resultatP():
    resultat = 1
    for x in range (1, y + 1):
        resultat = resultat * (x + 3 ** x) / (x + 5 ** (2 * x))
    return resultat
y = int (input("Vvedite Y: "))
print ("resultat:", resultatP())
